# /lib/utils - Shared utility helpers

General helpers reused across routes and components.

Examples:

- currency and date formatters
- enum label resolvers
- small pure helper functions

Guideline:

- Keep utilities pure, typed, and side-effect free.
