# `/lib` — Core Utilities & Configuration

Houses client initializations (Supabase, GraphQL), animation presets, font configs, and shared utility functions. This is the "brain" of your non-UI logic.

**📱 Mobile Analogy:** Like the **core/** or **data/datasource/** layer in Clean Architecture — where you set up Retrofit clients, Firebase init, shared preferences, and utility extensions.
