# `/lib/supabase` — Supabase Client & Helpers

Supabase client initialization (`createClient`) and helper functions for auth, database queries, and storage operations.

**📱 Mobile Analogy:** Like your **Firebase/Supabase singleton** in Flutter (`SupabaseClient`) or the `RemoteDataSource` class in Android Clean Architecture — the single point of contact with your BaaS.
