# /lib/supabase/queries - Read operations

Server-first query modules for Supabase read access.

Examples:

- fetch marketplace listings
- fetch creator jobs
- fetch UMKM order metrics

Guideline:

- Keep query contracts typed and colocated by domain.
