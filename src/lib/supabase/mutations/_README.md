# /lib/supabase/mutations - Write operations

Server-side mutation modules for Supabase writes.

Examples:

- create order
- update campaign status
- update profile

Guideline:

- Enforce validation before mutation and keep error mapping explicit.
