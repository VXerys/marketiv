# `/components` — Reusable UI Components

All shared, reusable React components organized by responsibility: atomic UI elements, page sections, layout primitives, and animation wrappers.

**📱 Mobile Analogy:** Like the **widgets/** folder in Flutter or a **ui/components/** package in Android — the building blocks (Buttons, Cards, Containers) that compose your screens.
